# Node.js
node_modules/
*.tgz

# Build output
dist/
build/

# Environment files (secrets — never commit)
.env
.env.local
.env.*.local

# Reconcile config + ledger + company profile (populated copies live in the
# references dir, e.g. ~/.config/elnora-merit; never commit local ones)
stripe-map.json
reconcile-ledger.json
company-profile.json

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Coverage
coverage/

# Logs
*.log

# Scratch / harvested docs
.firecrawl/
.docs-cache/
